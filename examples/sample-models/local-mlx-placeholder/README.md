# Local MLX Placeholder

This directory is a placeholder model path for validating the local MLX TTS workflow during MVP development.

It exists so the local provider can validate:

- model-path configuration
- local capability checks
- artifact-writing flow

It is not a real speech model bundle.
