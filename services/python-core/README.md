# Aodcast Python Core

Local orchestration core for Aodcast. It owns interview flow, script generation, provider dispatch, storage, the localhost HTTP runtime, and long-running task state.

See the repository root `README.md` for full setup and product documentation.
