{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aodcast.dev/schemas/script.schema.json",
  "title": "Script",
  "type": "object",
  "required": ["session_id", "script_id", "name", "draft", "final", "deleted_at", "revisions", "created_at", "updated_at"],
  "properties": {
    "session_id": { "type": "string" },
    "script_id": { "type": "string" },
    "name": { "type": "string" },
    "draft": { "type": "string" },
    "final": { "type": "string" },
    "deleted_at": { "type": ["string", "null"], "format": "date-time" },
    "revisions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["revision_id", "draft", "final", "reason", "created_at"],
        "properties": {
          "revision_id": { "type": "string" },
          "draft": { "type": "string" },
          "final": { "type": "string" },
          "reason": { "type": "string" },
          "created_at": { "type": "string", "format": "date-time" }
        },
        "additionalProperties": false
      }
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}
